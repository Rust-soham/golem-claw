# Agent Workspace

Use this folder for project-local agent assets: skills, helper notes, tool configs,
and any lightweight memory that should live with the repository.

Suggested layout:

- `skills/` - project-specific skills and workflows.
- `tools/` - small helper scripts or tool configuration for agents.
- `memory/` - durable notes that are safe to keep in version control.
